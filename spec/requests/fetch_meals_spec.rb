require 'rails_helper'

RSpec.describe "FetchMeals", type: :request do
  pending "GET /fetch_meals" do
    it "works! (now write some real specs)" do
      get fetch_meals_index_path
      expect(response).to have_http_status(200)
    end
  end
end
